# Load rules once and periodically evaluate them according to the global 'evaluation_interval'.
rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  #- job_name: "prometheus"

    # metrics_path defaults to '/metrics'
    # scheme defaults to 'http'.

    #static_configs:
      #- targets: ["localhost:9090"]
      
  #- job_name: 'kitexclient'
    #scrape_interval: 1s
    #metrics_path: /kitexclient
    #static_configs:
    #- targets: ['localhost:9091'] # scrape data endpoint
     
  - job_name: 'kitexserverA8080'
    scrape_interval: 1s
    metrics_path: /kitexserverA8080
    static_configs:
    - targets: ['localhost:9092'] # scrape data endpoint
  - job_name: 'kitexserverA8081'
    scrape_interval: 1s
    metrics_path: /kitexserverA8081
    static_configs:
    - targets: ['localhost:9093'] # scrape data endpoint
  - job_name: 'kitexserverA8082'
    scrape_interval: 1s
    metrics_path: /kitexserverA8082
    static_configs:
    - targets: ['localhost:9094'] # scrape data endpoint